@model ReffugeMVC.Models.TipoConteudo

@{
    ViewData["Title"] = "Details";
}

<h1>Detalhes</h1>

<div>
    <h4>Tipo de Conteúdo</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeTipoConteudo)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeTipoConteudo)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.TipoConteudoId" class="btn btn-segundo">Editar</a>
    <a asp-action="Index" class="btn btn-primary">Voltar para a lista</a>
</div>
