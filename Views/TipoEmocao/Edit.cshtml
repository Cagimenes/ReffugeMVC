@model ReffugeMVC.Models.TipoEmocao

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Tipo de Emoção</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TipoEmocaoId" />
            <div class="form-group">
                <label asp-for="NomeTipoEmocao" class="control-label">Tipo de Emoção</label>
                <input asp-for="NomeTipoEmocao" class="form-control" />
                <span asp-validation-for="NomeTipoEmocao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-segundo" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-primary">Voltar para a lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
