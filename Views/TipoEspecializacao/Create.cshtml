@model ReffugeMVC.Models.TipoEspecializacao

@{
    ViewData["Title"] = "Create";
}

<h1>Criar</h1>

<h4>Tipo da Especialização</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NomeTipoEspecializacao" class="control-label">Tipo da Especialização</label>
                <input asp-for="NomeTipoEspecializacao" class="form-control" />
                <span asp-validation-for="NomeTipoEspecializacao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Criar" class="btn btn-segundo" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-primary">Voltar para a lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
